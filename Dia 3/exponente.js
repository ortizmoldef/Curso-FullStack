function elevarAExponente(numero, exponente) {
    let resultado = Math.pow(numero, exponente);
    console.log(`El resultado de ${numero} elevado a ${exponente} es: ${resultado}`);
}

// Ejemplo de uso
const numero = 2;
const exponente = 3;
elevarAExponente(numero, exponente);